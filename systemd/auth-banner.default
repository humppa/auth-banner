# Configuration for auth-banner

# Path to auth log. Value must be present or
# auth-banner will fail to start.
AUTH_LOG_FILE=/var/log/auth.log

# Number of bytes to skip before tailing the log
# file. Usually 0. Some value must be present.
OFFSET=0
